#include<bits/stdc++.h>
using namespace std;
int n, s;
double a, b, c;
int main() {
    cin >> n;
    n*=2;
    a = 1/sqrt(5);
    b = pow((1+sqrt(5))/2, n);
    c = pow((1-sqrt(5))/2,n);

    return 0;
}