#include<bits/stdc++.h>
using namespace std;
int a[100], num, s;
int main() {
    cin >> num >> s;
    for (int i = 1; i <= num; i++){
        cin >> a[i];
    }
    int result = find(a, a + 5, s;
    cout << find(a, a + 5, s);
    return 0;
}